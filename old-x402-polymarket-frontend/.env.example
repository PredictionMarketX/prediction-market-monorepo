# X402 Integration Environment Variables

# Backend wallet private key (base58 encoded)
# This is the ONLY sensitive value - keep it secret!
# Generate with: solana-keygen new --outfile backend-wallet.json
# Then get base58 key with: solana-keygen display backend-wallet.json
X402_BACKEND_PRIVATE_KEY=your_base58_private_key_here

# NOTE: X402 payment address and facilitator URL are now configured in /app/configs/x402.ts
# Update those values directly in the config file instead of using environment variables
